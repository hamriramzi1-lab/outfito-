name: outfito
description: Assistant vestimentaire intelligent
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  geolocator: ^10.1.0
  weather: ^3.1.0
  table_calendar: ^3.0.9
  flutter_local_notifications: ^15.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
